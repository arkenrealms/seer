# /arken/packages/seer/packages/README.md

Module container for Seer runtime components.

## Folders
- `protocol/`: protocol schemas/transports and message contracts.
- `node/`: node-side Seer logic and integration points.

Both are managed as git submodules in this repo.
